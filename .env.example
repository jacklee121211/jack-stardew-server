# ============================================
# Puppy Stardew Server - Environment Variables
# 小狗星谷服务器 - 环境变量配置
# ============================================
#
# Copy this file to .env and fill in your values
# 复制此文件为 .env 并填写您的值
# cp .env.example .env
#

# ============================================
# Steam Credentials (REQUIRED)
# Steam 凭证（必需）
# ============================================
# IMPORTANT: You MUST own Stardew Valley on Steam!
# 重要：您必须在 Steam 上拥有星露谷物语！
STEAM_USERNAME=your_steam_username
STEAM_PASSWORD=your_steam_password

# ============================================
# VNC Settings (OPTIONAL)
# VNC 设置（可选）
# ============================================
# Enable VNC for initial setup (recommended for first run)
# 启用 VNC 进行初始设置（首次运行推荐）
ENABLE_VNC=true
VNC_PASSWORD=stardew123

# ============================================
# Proxy Settings (OPTIONAL)
# 代理设置（可选）
# ============================================
# Use proxy to accelerate Steam downloads
# 使用代理加速 Steam 下载
#
# Format: http://IP:PORT or http://hostname:PORT
# 格式: http://IP:端口 或 http://主机名:端口
#
# Examples:
# - Clash Meta on Docker gateway: http://172.17.0.1:7890
# - Clash Meta on host IP: http://10.0.13.4:7890
# - Custom proxy: http://proxy.example.com:8080
#
# Leave empty to disable proxy (default)
# 留空以禁用代理（默认）
HTTP_PROXY=
HTTPS_PROXY=

# If you set HTTP_PROXY, HTTPS_PROXY will use the same value if left empty
# 如果设置了 HTTP_PROXY，HTTPS_PROXY 留空时会使用相同的值
